{
  "name": "media-finder-extractor",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone, SSRF-aware media extraction API for Media Finder",
  "type": "module",
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "test": "node --test",
    "test:watch": "node --test --watch"
  },
  "dependencies": {
    "cheerio": "^1.1.0"
  },
  "optionalDependencies": {
    "playwright-core": "^1.55.0"
  }
}
